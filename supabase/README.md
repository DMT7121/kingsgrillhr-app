# Supabase handoff
Copy `schema.sql` and `seed.sql` from the full package technical folder, then run them in Supabase SQL Editor. This starter includes `services/supabase-client.ts` and `services/hr-api.ts` for incremental replacement of mock data.
